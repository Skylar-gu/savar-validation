# Python 3.9 (dev env: 3.9.25). Pinned to the versions the shipped numbers were produced with.
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.9.4
tigramite==5.2.10.1        # PCMCI / PCMCI+ (ParCorr)
pydmd==2025.8.1            # bakeoff/discover_modes.py, DMD candidate only (imported lazily)

# torch was installed as the CUDA 12.4 build (2.6.0+cu124). PyPI only serves the
# CPU/generic wheel; for the GPU build (CNN/GNN/SAE training) install with:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0

# The upstream SAVAR library is a separate clone, not a pip dependency — see README:
#   git clone https://github.com/xtibau/savar.git savar && git -C savar checkout 532e5e5
