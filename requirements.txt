# Python 3.9 (dev env: 3.9.25)
# Pinned to the versions used for the diurnal/annual validation run.

numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.9.4
tigramite==5.2.10.1

# torch was installed as the CUDA 12.4 build (2.6.0+cu124). PyPI only serves the
# CPU/generic wheel; for the GPU build (CNN/SAE training) install with:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0

# Upstream SAVAR library is a separate clone, not a pip dependency — see README:
#   git clone https://github.com/xtibau/savar.git savar

# ── Alternative causal discoverers (pcmci/run_baselines.py) ────────────────────
# TSCI: vendored into pcmci/methods/tsci/ (MIT, KurtButler/tangentspaces) — runs on
#   the deps above, no extra install.
# DYNOTEARS: causalnex pins numpy<1.24 / pandas<2.0 (incompatible with numpy 2.0),
#   so it runs in an ISOLATED venv, never imported into the main env:
#     python3.9 -m venv .venv_causalnex && .venv_causalnex/bin/pip install causalnex
#   The adapter (pcmci/methods/dynotears.py) shells out to it.
